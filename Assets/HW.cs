using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HW : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello this is the start");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Hello this is the start");
    }
}
